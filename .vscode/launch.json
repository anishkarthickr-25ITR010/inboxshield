{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Standalone Preview",
      "file": "${workspaceFolder}/index_standalone.html"
    },
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Vite App (http://localhost:3000)",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
